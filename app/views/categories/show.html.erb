<h1><%= @category.title %></h1>

<h2>Movies</h2>
<table>
  <% @category.films.each do |film| %>
   <tr>
      <td> <%= film.ratings.first.rating %></td>
     <td><%= film.title %></td>
    </tr>
  <% end %>
</table>
