<%= form_for [@film, @review] do |f| %>
  <% if @review.errors.any? %>
    <div id = "errors">
     <h2> <%= pluralize(@review.errors.count, "error") %> prevented this review from saving </h2>
     <ul>
      <% @review.errors.full_messages.each do |msg| %>
      <li> <%= msg %></li>
      <% end %>
    </ul>
    </div>
  <% end %>
  <p>
   <%= f.label :body %>
   <%= f.text_field :body %>
  </p>
  <p>
    <%= submit_tag("Submit Review") %>
  </p>
<% end %>
